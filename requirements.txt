# Python dependencies for No-Show Prediction ML Pipeline
# Install with: %pip install -r requirements.txt

# Machine Learning & Data Processing
scikit-learn>=1.3.0  # For TargetEncoder and preprocessing utilities
pandas>=2.0.0        # Data manipulation (used in target encoding workflow)

# Note: The following are pre-installed on Databricks and do NOT need to be listed:
# - pyspark (Spark ML for distributed model training)
# - mlflow (experiment tracking and model registry)
# - numpy (numerical computing)
# - matplotlib (basic plotting)